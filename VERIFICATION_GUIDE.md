# Guide verification

Date: 2026-09-04

The first-round flow was verified locally as fullscreen landing → rules → player setup → Start challenge → visual How to Play popup. The popup contains the generated target-color/orb visual, exact HTML overlays explaining that the target color appears above, the player taps the matching orb, and correct/wrong scoring is +5/−3. The popup CTA reads `Got it — start in 3s`. The image framing was adjusted to use the full visual asset rather than cropping the target or tap area. Acknowledging the guide then starts the existing black 3-2-1-GO countdown.
