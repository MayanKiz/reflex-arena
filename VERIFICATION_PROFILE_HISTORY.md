# Grouped profile verification

Date: 2026-09-05

The full leaderboard opened from the rules-page `View all` action. Profile rows showed rank, total runs, average accuracy, top score, and a clickable arrow. Clicking `Test Player` opened a profile panel with `top score`, `total games`, `avg accuracy`, and two historical runs with timestamps and scores. The local cached/fallback data merged the two same-name runs into one profile.
